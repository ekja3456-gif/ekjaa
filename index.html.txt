<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>포트폴리오 타임라인</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 40px 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        h1 {
            text-align: center;
            color: white;
            font-size: 3em;
            margin-bottom: 50px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
        }

        .timeline {
            position: relative;
        }

        .year-section {
            margin-bottom: 60px;
            opacity: 0;
            animation: fadeIn 0.6s forwards;
        }

        .year-section:nth-child(1) { animation-delay: 0.1s; }
        .year-section:nth-child(2) { animation-delay: 0.3s; }
        .year-section:nth-child(3) { animation-delay: 0.5s; }

        @keyframes fadeIn {
            to {
                opacity: 1;
            }
        }

        .year-header {
            background: white;
            padding: 20px 30px;
            border-radius: 15px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.2);
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .year-header:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(0,0,0,0.3);
        }

        .year-header h2 {
            font-size: 2.5em;
            color: #667eea;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .toggle-icon {
            font-size: 0.8em;
            transition: transform 0.3s ease;
        }

        .year-header.collapsed .toggle-icon {
            transform: rotate(180deg);
        }

        .semesters {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 25px;
            max-height: 1000px;
            overflow: hidden;
            transition: max-height 0.4s ease, opacity 0.4s ease;
        }

        .semesters.collapsed {
            max-height: 0;
            opacity: 0;
        }

        .semester-card {
            background: white;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 8px 25px rgba(0,0,0,0.15);
            transition: all 0.3s ease;
            border-left: 5px solid #667eea;
        }

        .semester-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 15px 35px rgba(0,0,0,0.25);
            border-left-width: 8px;
        }

        .semester-card h3 {
            color: #764ba2;
            font-size: 1.8em;
            margin-bottom: 15px;
        }

        .project-list {
            list-style: none;
            margin-top: 20px;
        }

        .project-item {
            padding: 12px 0;
            border-bottom: 1px solid #f0f0f0;
            color: #555;
            font-size: 1.1em;
            transition: all 0.2s ease;
        }

        .project-item:last-child {
            border-bottom: none;
        }

        .project-item:hover {
            color: #667eea;
            padding-left: 10px;
        }

        .add-btn {
            background: #667eea;
            color: white;
            border: none;
            padding: 10px 20px;
            border-radius: 8px;
            cursor: pointer;
            margin-top: 15px;
            font-size: 1em;
            transition: all 0.3s ease;
        }

        .add-btn:hover {
            background: #764ba2;
            transform: scale(1.05);
        }

        @media (max-width: 768px) {
            h1 {
                font-size: 2em;
            }

            .year-header h2 {
                font-size: 1.8em;
            }

            .semesters {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>📚 나의 포트폴리오</h1>
        
        <div class="timeline">
            <div class="year-section">
                <div class="year-header" onclick="toggleYear(this)">
                    <h2>2025 <span class="toggle-icon">▼</span></h2>
                </div>
                <div class="semesters">
                    <div class="semester-card">
                        <h3>1학기</h3>
                        <ul class="project-list">
                            <li class="project-item">프로젝트를 추가해보세요</li>
                        </ul>
                        <button class="add-btn" onclick="addProject(this)">+ 프로젝트 추가</button>
                    </div>
                    <div class="semester-card">
                        <h3>2학기</h3>
                        <ul class="project-list">
                            <li class="project-item">프로젝트를 추가해보세요</li>
                        </ul>
                        <button class="add-btn" onclick="addProject(this)">+ 프로젝트 추가</button>
                    </div>
                </div>
            </div>

            <div class="year-section">
                <div class="year-header" onclick="toggleYear(this)">
                    <h2>2026 <span class="toggle-icon">▼</span></h2>
                </div>
                <div class="semesters">
                    <div class="semester-card">
                        <h3>1학기</h3>
                        <ul class="project-list">
                            <li class="project-item">프로젝트를 추가해보세요</li>
                        </ul>
                        <button class="add-btn" onclick="addProject(this)">+ 프로젝트 추가</button>
                    </div>
                    <div class="semester-card">
                        <h3>2학기</h3>
                        <ul class="project-list">
                            <li class="project-item">프로젝트를 추가해보세요</li>
                        </ul>
                        <button class="add-btn" onclick="addProject(this)">+ 프로젝트 추가</button>
                    </div>
                </div>
            </div>

            <div class="year-section">
                <div class="year-header" onclick="toggleYear(this)">
                    <h2>2027 <span class="toggle-icon">▼</span></h2>
                </div>
                <div class="semesters">
                    <div class="semester-card">
                        <h3>1학기</h3>
                        <ul class="project-list">
                            <li class="project-item">프로젝트를 추가해보세요</li>
                        </ul>
                        <button class="add-btn" onclick="addProject(this)">+ 프로젝트 추가</button>
                    </div>
                    <div class="semester-card">
                        <h3>2학기</h3>
                        <ul class="project-list">
                            <li class="project-item">프로젝트를 추가해보세요</li>
                        </ul>
                        <button class="add-btn" onclick="addProject(this)">+ 프로젝트 추가</button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        function toggleYear(header) {
            const semesters = header.nextElementSibling;
            header.classList.toggle('collapsed');
            semesters.classList.toggle('collapsed');
        }

        function addProject(btn) {
            const projectName = prompt('프로젝트 이름을 입력하세요:');
            if (projectName && projectName.trim()) {
                const list = btn.previousElementSibling;
                const firstItem = list.querySelector('.project-item');
                
                if (firstItem.textContent === '프로젝트를 추가해보세요') {
                    firstItem.textContent = projectName.trim();
                } else {
                    const li = document.createElement('li');
                    li.className = 'project-item';
                    li.textContent = projectName.trim();
                    list.appendChild(li);
                }
            }
        }
    </script>
</body>
</html>